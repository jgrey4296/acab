"""
Extension classes for PyParsing

Provides:
A ForwardParser that combines parsers inserted into it,
instead of overwriting.

A Core parameterised parser for values/bindings.

A Statement Parser which wraps provided parsers into a standardised form.

"""
