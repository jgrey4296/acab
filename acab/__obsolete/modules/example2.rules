activity:

    // VP suff?
    subject, tool, object -> outcome
    rules commmunity div_of_labour

end
artifact:

    creation
    materials
    variance
    fungible
    capabilities

end
lattice:

    top, bottom
    nodes
    // use meet and join operators

end
pipeline:
    a.layer.$set(::Layer, collect)?
    other.layer.$set2(::Layer, collect)?

    λrun $set
    loop(5):
        λrun $set2
    end
end
game.social:
    constraints
    metrics
    protocol
    artifacts
    roles
    actions
end
game.theoretic:
    roles
    action matrix /tree
    payoffs
end
space:
    // hex | square | continuous | DCEL | Spreadsheet
    markers / positions

end
time:
    // tidal patterns
    // [a $b c, $d [e f]]
end
