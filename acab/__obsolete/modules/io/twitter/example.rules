twitter.seup:
    #twitter
    ~twitter.connection?
    config.twitter.${secrets}(::file)?

    λtwitter.init $secrets -> $connection

    twitter.$connection
    λctx.tag.bind #twitter tweet λmodule.twitter.tweet $connection
end

// TODO linear logic consume modality
// could have consumption as list (a.b.c), or set (a.b, a.c, a.d..)
sending.tweet.rule:
    #twitter
    to.send(::logic.linear) o- $msg?

    λtweet $msg
end
