// initial assertions
a.b!a

format.string."{x} : {y}"

// A Transform rule
test.rule: (::ρ)
  format.string.$a?
  a.$x!$y?

  \operator.transform.n_ary.format $a -> $result
  
  operator.action.add(output.$result)
end

