// initial assertions
a.b!a
a.c!b
a.d!c

format.first."{x} : {y}"
format.second."{result} test"

// A Shift rule
test.rule: (::ρ)
  format.first.$a?
  format.second.$b?
  a.$x!$y?

  \operator.transform.n_ary.format $a -> $result
  \operator.transform.n_ary.format $b -> $result_b

  operator.action.add(output.$result_b)
end

